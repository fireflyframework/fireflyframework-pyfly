"""{{ names.pascal }} web controller — serves HTML pages."""

from pyfly.container import controller
from pyfly.web import ModelAndView, get_mapping, request_mapping

@controller
@request_mapping("/{{ names.kebab }}")
class {{ names.pascal }}Controller:

    @get_mapping("/")
    async def index(self) -> ModelAndView:
        return ModelAndView("{{ names.snake }}.html")
